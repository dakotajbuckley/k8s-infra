apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: test-cert
  namespace: default
spec:
  secretName: test-tls
  issuerRef:
    name: letsencrypt-azure
    kind: ClusterIssuer
  dnsNames:
    - test.dakotabuilds.dev