apiVersion: v1
kind: Service
metadata:
  name: test-pod
  labels:
    app: test-pod
spec:
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30002
  type: NodePort
  selector:
    app: test-pod
