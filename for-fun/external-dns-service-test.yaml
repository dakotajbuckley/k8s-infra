apiVersion: v1
kind: Service
metadata:
  name: external-dns-test
  annotations:
    external-dns.alpha.kubernetes.io/hostname: test.dakotabuilds.dev
    external-dns.alpha.kubernetes.io/target: 1.2.3.4
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 80